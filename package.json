{
  "name": "pi-system-prompt-tweaks",
  "version": "0.1.0",
  "private": true,
  "description": "Small, targeted, update-safe corrections to Pi's built-in system prompt",
  "keywords": ["pi-package", "pi-extension"],
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Dropje97/pi-system-prompt-tweaks.git"
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.4",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
